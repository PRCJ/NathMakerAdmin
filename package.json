{
    "name": "nathmaker-admin",
    "version": "1.0.0",
    "scripts": {
        "build": "echo 'Frontend pre-built and committed in public/'",
        "build:local": "./gradlew prepareVercelDist && mkdir -p public && cp -r frontend/build/vercel-dist/* public/"
    },
    "dependencies": {},
    "devDependencies": {}
}